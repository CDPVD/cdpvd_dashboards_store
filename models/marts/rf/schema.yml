version: 2

models:
  - name: fact_solde_el
    config:
      tags:
        - rf
      schema: "rf"
    description: >
      Table de fait qui fournit l'historique du solde 
    tests:
      - core_dashboards_store.resolution:
          combination_of_columns:
            - code_perm
            - fiche
            - annee

  - name: fact_contact
    config:
      tags:
        - rf
      schema: "rf"
    description: >
      Table de fait qui fournit l'ensemble des adresses des fiches
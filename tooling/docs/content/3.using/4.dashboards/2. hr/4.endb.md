---
title: Enseignant(e) détenteur d'un brevet
---

::alert{type=warning}
Suivi du nombre d'enseignant(e)s détenteur d'un brevet ou non
::

# Déploiement

:badge[tag:endb]{type="success"}
:badge[new in v0.11.0]

## Bases de données

La base de données `paie` doit être liée au projet. Veuillez vous référer à la section [Lier une base de données](/using/configuration/databases) pour plus d'informations sur la façon de lier une base de données.

## Seed

::alert{type=warning} 3 seeds sont requises pour le fonctionnement du tableau de bord. ::

Le tableau de bord nécessite les 3 seeds suivantes :  ens_qualification, statut_enseignant et secteur. Veuillez consulter la section sur l'amorçage [seeding](/using/configuration/adapts-seeds) pour plus d'informations sur l'amorçage d'un mart. Ces seeds sont nécessaires pour peupler les sujets sanctionnées. Comme vous n'aurez pas besoin de la remplacer, vous pouvez simplement exécuter la commande suivante pour créer la seed : dbt seed --select +tag:endb. Les seeds sont décrites à cet endroit : seeds/marts/human_resources/schema.yml

 - ens_qualification : Obligatoire : liste des codes de qualifications, la description ainsi qu'un bool pour savoir si on concidère comme étant qualifié ou non. Cette information peut-être demandée aux ressources humaine pour les types de qualifications
 
 - statut_enseignant : Obligatoire : Ce sont les statuts d'enseignant, habituellement, ce sont les statuts débutant par E de la table PAI_TAB_STAT_ENG  et complétez l'information du statut, Titulaire, Remplaçant, Suppléant, ... selon ce que vous voulez.
 
 - secteur : Obligatoire : Liste des numéros d'unité administrative, un secteur (comme un quartier, ou le regroupement selon votre CSS) ainsi que l'odre d'enseignemant (FFJ, FGA, FP, ...)

## Marts

Les marts suivants doivent être activés pour que le tableau de bord fonctionne. Veuillez vous référer à la section [Activer un mart](/using/configuration/enabling) pour plus d'informations sur la façon d'activer un mart.

- `human_resources`

## Dbt project specification
> Update your `cssxx.dashboards_store/dbt_project.yml` file with the following snippet.

```yaml
# cssxx.dashboards_store/dbt_project.yml
models: 
  store:
    marts:
      human_resources:
        +enabled: True  
    dashboards: 
      endb:
        +enabled: True
    interfaces:
      paie:
        +enabled: True
```
version: 2

models:
  - name: stg_unit_adm
    description: >
      Table qui recupere les lieux de travail administratifs (code et description) pour l'exercice en cours.
    tests:
      - core_dashboards_store.resolution:
          combination_of_columns:
            - code
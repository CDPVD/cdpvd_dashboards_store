---
title: Service éducatif des adultes
---

::alert{type=info}
**Seeds et adaptateurs.** Veuillez vous réferer à la documentation [des seeds et adaptateurs](/using/configuration/adapts-seeds) pour obtenir l'information concernant la méthode d'implantation des seeds et adaptateurs. 
::

# Seeds

Il n'y a pas de seeds obligatoires pour le service éducatif des adultes.

# Adaptateurs

## Adaptateurs obligatoires
::alert{type=warning}
Ces `adaptateurs` doivent être populer pour le bon fonctionnement de l'ETL.
::

Le tableau suivant montre la définition des adaptateurs requis et le chemin où les `sql` doivent être.

| Where to save it |  name of the adapter to implement |
| --- | --- |
| `cssXX.dashboards_store/models/marts/educ_serv_adultes/staging/populations/*.sql` | stg_ele_fga, stg_ele_fp, stg_populations_adultes |

::alert{type=warning}
Veuillez vous réferer à la [documentation des adaptateurs](/using/configuration/adapts-seeds) pour obtenir l'information sur la manière dont les adapteurs doivent être implémentés.
::

### Adaptateurs de population

Afin de construire votre population, vous devez définir pour chaque population les règles d'affaire pour les années précedente y compris l'année en cours. 


Dans votre `cssXX.dashboards_store`:
1. Créez autant de fichiers `.sql` que vous avez de populations. Ces fichiers doivent contenir les mêmes colonnes que le fichier `stg_populations_adultes.sql`.

```sql
# cssXX.dashboards_store/models/marts/educ_serv_adultes/staging/populations/stg_populations_adultes
select code_perm, fiche, annee, freq, 'Formation professionnelle' as population
from {{ ref("stg_ele_fp") }} 
union
select code_perm, fiche, annee, freq, 'Formation générale des adultes' as population
from {{ ref("stg_ele_fga") }}
```

2. Créez le fichier `schema.yml` permettant de faire les tests d'acceptations et unitaires.
```yaml
# cssXX.data.store/models/marts/educ_serv_adultes/populations/staging/schema.yml
version: 2

models:
  - name: stg_populations_adultes
    config:    
      tags:
        - population
        - educ_serv_adultes
    description: table de fait qui identifie les fréquentations FP-FGA
    tests:
      - resolution:
          combination_of_columns:
            - code_perm
            - fiche
            - annee
            - freq
  - name:  stg_ele_fga
    config:    
      tags:
        - population
        - educ_serv_adultes
    description: >
          Identifier les inscriptions en FGA
    tests: 
      - resolution: 
          combination_of_columns: 
            - code_perm
            - fiche
            - annee
            - freq
  - name:  stg_ele_fp
    config:   
      tags:
        - population
        - educ_serv_adultes
    description: >
          Identifier les inscriptions en FP
    tests: 
      - resolution: 
          combination_of_columns: 
            - code_perm
            - fiche
            - annee
            - freq
```